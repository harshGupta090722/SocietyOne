# Default / Production environment variables (Vercel)
# API requests go to /api/v1 on the same origin, which are proxied via vercel.json to the EC2 backend.
# Local development overrides are in .env.development.

VITE_API_URL=/api/v1